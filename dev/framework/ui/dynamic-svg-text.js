(()=>{'use strict';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const presets={number:{shape:'rounded',font_weight:800},time:{shape:'rounded',font_weight:800},unit:{shape:'soft',font_weight:700},currency:{shape:'pill',font_weight:800},operator:{shape:'square',font_weight:800},label:{shape:'rounded',font_weight:700}};const defaults={shape:'rounded',padding_x:5,padding_y:3,min_width:18,height:24,font_size:11,font_family:'system-ui, sans-serif',font_weight:800,foreground:'currentColor',background:'transparent',border:'currentColor',border_width:1.5,radius:6};const widthFor=(text,cfg)=>Math.max(cfg.min_width,Math.ceil([...String(text)].length*cfg.font_size*.68+cfg.padding_x*2));const radiusFor=(cfg,w)=>cfg.shape==='pill'?cfg.height/2:cfg.shape==='square'?0:cfg.shape==='soft'?Math.min(4,cfg.radius):Math.min(cfg.radius,cfg.height/2,w/2);function normalize(options={}){const p=options.preset&&presets[options.preset]?presets[options.preset]:{};return{...defaults,...p,...options};}function render(text,options={}){const cfg=normalize(options),value=String(text??''),w=options.width||widthFor(value,cfg),h=cfg.height,r=radiusFor(cfg,w),bg=cfg.background==='transparent'?'none':cfg.background;return `<svg class="nlab-dynamic-text-svg" viewBox="0 0 ${w} ${h}" width="${w}" height="${h}" role="img" aria-label="${esc(options.aria_label||value)}" xmlns="http://www.w3.org/2000/svg"><rect x=".75" y=".75" width="${Math.max(0,w-1.5)}" height="${Math.max(0,h-1.5)}" rx="${r}" fill="${esc(bg)}" stroke="${esc(cfg.border)}" stroke-width="${cfg.border_width}"/><text x="${w/2}" y="${h/2+.5}" text-anchor="middle" dominant-baseline="middle" font-family="${esc(cfg.font_family)}" font-size="${cfg.font_size}" font-weight="${cfg.font_weight}" fill="${esc(cfg.foreground)}">${esc(value)}</text></svg>`;}function element(text,options={}){const t=document.createElement('template');t.innerHTML=render(text,options).trim();return t.content.firstElementChild;}function dataUri(text,options={}){return 'data:image/svg+xml;charset=utf-8,'+encodeURIComponent(render(text,options));}window.NLabSVGText={render,element,dataUri,normalize,presets:{...presets}};})();
